#include "STOCKS_h.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void printStocks(struct stocks_type stocksPortfolio[], int SIZE);

int main(void) {
  file *stocksPTR = fopen("stocks.csv","r");

  if (stocksPTR == NULL) {

    printf("file could not be found");

    return -1;
    
  }

  int a, b, SIZE = 3200;
  
  struct stocks_type = stocksList[SIZE];

  fscanf(stocksPTR, "%s,%lf,",
	 &stock->symbol,
	 &stock->price);

  printf("%s %lf");

  
  
  return 0;
}

void printStocks(struct stocks_type stocksPortfolio[], int SIZE) {

}
