#ifndef STOCKS_H
#define STOCKS_H

struct stocks_type {
  char symbol[3200];
  char price[3200];
};

typedef  struct stocks_type *  Stock;
Client create (void);
void getSymbol(Stock stock);
void getPrice(Stock stock);


#endif
